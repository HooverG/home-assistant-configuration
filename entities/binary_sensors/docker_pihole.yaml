# Command used : curl -s -o /dev/null -w '%{http_code}' http://ip_address/admin/

platform: command_line
command: !secret docker_pihole_curl
name: 'Docker Pi-hole'
device_class: connectivity
payload_on: 200
scan_interval: 300
command_timeout: 30